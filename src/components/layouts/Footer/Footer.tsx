import { FC } from 'react'
import { Container } from '@/components/ui'

const Footer: FC = () => {
    return (
        <footer className="bg-slate-100">
            <Container>ini main layout footer</Container>
        </footer>
    )
}

export default Footer
